> # Welcome to Tour Api
>
> `Product Schema `

`// Info for POST endpoints`
```
{
    name: String,
    description: String,
    price: Number,
    duration: Number,
    maxGroupSize: Number,
    status: String,

}
```
